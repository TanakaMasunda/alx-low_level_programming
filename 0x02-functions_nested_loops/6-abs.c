#include "main.h"

/**
 * _abs - absolute value of an integer
 *
 * @r: check code 
 *
 * Return: Always 0 (success)
 */

int _abs(int)

{
	int r;

	if (r < 0)
	{
		r = (-1)* r;
		_putchar(r);
	}
	else
	{
		_putchar(r);
	}
	return (0);
}
