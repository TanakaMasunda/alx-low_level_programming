#include "main.h"

/**
 * mul - multiply 2 intergers
 * @a: first interger
 * @b: second integer
 * Return: 0  if succes
 */

int mul(int a, int b)
{
	int i;

	i = (a * b);
	printf("%d\n", i);

	return (0);
}
