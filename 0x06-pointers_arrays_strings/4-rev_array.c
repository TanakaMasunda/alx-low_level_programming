#include "main.h"

/**
 * reverse_array - print array in reverse
 * @a: first integer
 * @n: second integer
 * Return: 0 if success
 */

void reverse_array(int *a, int n)
{
	int i;

	int array[n];

	for (i = 0; i < n && i != 0; i--;)
		putchar (i);
	return (0);
}
